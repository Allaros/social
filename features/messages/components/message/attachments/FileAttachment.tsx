import { MessageAttachment } from '@/features/messages/types/messages.types';
import React from 'react';

const FileAttachment = ({ file }: { file: MessageAttachment }) => {
   return <div>FileAttachment</div>;
};

export default FileAttachment;
